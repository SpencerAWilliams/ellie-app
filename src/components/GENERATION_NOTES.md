# Generation notes — Desktop1

Generated from `src/tokens/figma_schema.json` (Figma component **"Desktop - 1"**).

## ⚠️ Missing skill

The CI prompt instructs the agent to *"follow the figma-to-code skill"* and *"use
the API path code generation rules from the skill."* **No such skill is present**
in this repository, in the Claude Code skills registry, or anywhere on the CI
filesystem (only `scripts/figma/extract.py` and the two workflows reference it).

The component below was therefore generated by inferring the intent rather than
following authored rules:

- **"API path"** is read as the **Figma REST API extraction path** implemented by
  `scripts/figma/extract.py` (a single `/v1/files/:key` call), as opposed to a
  Figma-plugin / MCP path.
- Output is a self-contained React component plus CSS, driven by the extracted
  design tokens.

If a real `figma-to-code` SKILL.md is expected to govern framework choice, file
naming, and layout strategy, it needs to be authored and committed — then this
output should be regenerated to match it.

## Known limitation of the API path

`extract.py` records each node's **size and tokens but not its absolute x/y
position**, and the source frame uses no Figma auto-layout. Children are laid out
in document order with flexbox rather than pixel-positioned. This is the most
faithful result the REST API path can produce without positional data.

## Files

- `tokens.css` — design tokens (`:root` custom properties)
- `Desktop1.css` — component styles referencing the tokens
- `Desktop1.jsx` — React component; every schema node is rendered with its
  inferred tag and a `data-node-id` back-reference to Figma
